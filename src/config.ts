// Costanti UI
export const COMBOBOX_MAX_VISIBLE = 200;
export const TOAST_TTL_MS = 4000;
export const LOGIN_HINT_TTL_MS = 2000;

// Default per quando le settings dal backend non sono ancora arrivate
export const DEFAULT_MAX_HOURS = 8;
export const DEFAULT_MIN_HOURS = 0;
export const DEFAULT_HOURS_INTERVAL = 0.5;
export const DEFAULT_DAYS_BEFORE = 120;
